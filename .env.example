# Copy to .env and replace with credentials for your Postgres instance.
# Include ?sslmode=require when using Neon or any TLS-enabled host.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kdrama_journal?sslmode=require
